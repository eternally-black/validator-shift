module.exports = {
  root: true,
  parser: '@typescript-eslint/parser',
  plugins: ['@typescript-eslint'],
  extends: ['eslint:recommended', 'plugin:@typescript-eslint/recommended'],
  parserOptions: { ecmaVersion: 2022, sourceType: 'module' },
  ignorePatterns: [
    'dist',
    'node_modules',
    '.next',
    'build',
    // Auto-generated by Next.js — uses /// reference syntax we can't change.
    'packages/web/next-env.d.ts',
  ],
  rules: {
    // Underscore-prefixed args are the conventional "intentionally unused"
    // marker — and we use it for callback-shape conformance (e.g. event
    // payload arg the orchestrator emits but the test doesn't assert on).
    '@typescript-eslint/no-unused-vars': [
      'error',
      { argsIgnorePattern: '^_', varsIgnorePattern: '^_' },
    ],
    // We intentionally use the class+interface declaration-merging pattern
    // to publish strongly-typed event maps (see MigrationOrchestrator).
    // The "unsafe" warning is about untyped emit() calls, which we don't
    // do — every emit() call is type-checked through the merged interface.
    '@typescript-eslint/no-unsafe-declaration-merging': 'off',
  },
  overrides: [
    {
      // Project-level config files in CommonJS — `require`, `module`,
      // `__dirname` are all expected globals here.
      files: [
        '**/*.config.js',
        '**/*.config.cjs',
        '**/.eslintrc.cjs',
        '**/postcss.config.js',
      ],
      env: { node: true, commonjs: true },
      rules: {
        '@typescript-eslint/no-var-requires': 'off',
        '@typescript-eslint/no-require-imports': 'off',
      },
    },
  ],
}
